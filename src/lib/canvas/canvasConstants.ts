// Board size. 1600×3200 keeps the two trees close enough that a phone can
// frame both (the old 2400px board put an empty 700px band between them —
// "Fit" could never fit, and drag-to-Final crossed half a kilometer of void).
export const CANVAS_WIDTH = 1600;
export const CANVAS_HEIGHT = 3200;
export const DIVIDER_X = CANVAS_WIDTH / 2;
export const IDEAS_ZONE_WIDTH = CANVAS_WIDTH / 2;
export const FINAL_ZONE_WIDTH = CANVAS_WIDTH / 2;
