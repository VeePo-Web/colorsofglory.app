/** Short tap — confirms a recording has started. */
export const HAPTIC_RECORD_START = [10];

/** Tap-pause-tap — confirms a recording has stopped and is being prepared for review. */
export const HAPTIC_RECORD_STOP = [10, 40, 10];
