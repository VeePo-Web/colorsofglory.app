# E4 Credits — Progress Log

## Step 1 — Route un-redirected; /songs/:id/credits reachable (2026-07-07)
- **Route change (A5-lane, filed + landed):** `src/routes/songRoutes.tsx` — `/songs/:id/credits` no longer redirects to `CanvasLayerRedirect layer="people"`; it now renders `<RequireAuth><CreditsPage /></RequireAuth>` (lazy, code-split). One route line + one lazy import; nothing else in the route tree touched.
- **Timing note for A5:** at step start, `App.tsx` still carried the OLD inline route table (including the credits redirect) and did not mount the fragments — A5's shell rewrite landed *mid-step* (App.tsx now composes `{songRoutes}` etc. via AnimatedRoutes). Result: the songRoutes.tsx edit IS the single live route surface; **no App.tsx edit was needed or made.** The stale inline copy of the redirect is gone with A5's rewrite.
- **Page:** `src/pages/CreditsPage.tsx` untouched this step — the warm scaffold (glow, serif "Credits", "Every contribution remembered.", owner-gold card, contribution chips, Export button) kept as-is. Still the hardcoded mock + dead Export; that's Steps 2–5.
- **Framing/guarding:** page self-frames (`var(--max-w-app)` centered column, cream bg, warm glow) matching the app frame; route guarded by `RequireAuth` like every other `/songs/:id/*` surface.
- **Verification (real checks, not assumed):**
  - New `src/test/credits-route.test.tsx` (2/2 pass): (a) structural — the credits route element's type IS `RequireAuth` (guard present); (b) behavioral — mounting the real `songRoutes` fragment at `/songs/s1/credits` in a MemoryRouter renders the ledger (serif "Credits" heading, "Every contribution remembered.", Export button) — with the old redirect this would have bounced to the canvas route and rendered none of it.
  - Neighbor tests green: `song-workspace-hub` 7/7, `codex-qa-config` 2/2 (QA route list already includes `/songs/1/credits` — unchanged).
  - `npx tsc --noEmit` exit 0; `npx vite build` exit 0 — bundle now emits a dedicated `CreditsPage-*.js` chunk (proof the lazy page is wired).
  - Full-browser drive not possible headless (no playwright/puppeteer installed; PasswordGate + Supabase auth in front) — jsdom route render is the behavioral evidence this step.
- **Pre-existing failure, NOT mine:** `design-guard.test.ts` fails on `components/ui/glow.tsx: #B8953A` (raw brand hex, another lane's untracked file). My diff contains no color values. Flagging for A1/design lane.
- **Depends on now:** A5's shell composition (App.tsx → `{songRoutes}`); `RequireAuth` (whatever A4/A5 evolve it to — the route wraps the module export, not its internals).
- **Next (Step 2):** file `getSongCredits(songId)` for A3 + `SongCredit` for A2 in `docs/CREDITS-CONTRACT.md`. Noted: `src/types/credit.ts` already exists as an A2 placeholder ("SONGWRITING CREDITS (GREENFIELD)") with the hard money/creative separation comment — start there.

## Step 1 addendum — survived the working-tree restore (2026-07-08)
- A fleet-wide "Restore working tree" pass **deleted the modular route fragments** (`src/routes/songRoutes.tsx` et al.) and returned `App.tsx` to inline routes. **The credits un-redirect survived**: the restored `App.tsx` carries `/songs/:id/credits → <RequireAuth><CreditsPage /></RequireAuth>` (real page, lazy, guarded) — committed and pushed on `main`. Sibling panel routes (activity, people, voice, notes) reverted to `CanvasLayerRedirect`; credits did not.
- The restore also brought an **evolved `CreditsPage`**: no longer the hardcoded mock — it derives credits from the song room (`deriveCredits` in `src/lib/canvas/credits`), enriches with the live roster (`useSongCollaborators`) for role/color/owner-crown, sorts owner-first then by contribution volume, has a real empty state ("No contributions yet"), and Export copies a plain-text credits block to the clipboard. Steps 2–5 should re-audit against this reality (much is already ahead of the original charter's "hardcoded orphan" map; the A3 `getSongCredits` contract filing still stands).
- `src/test/credits-route.test.tsx` was deleted by the restore (it imported the removed fragment). **Recreated** against the inline-routes architecture: (a) source-level wiring guard on `App.tsx` (credits route line must contain `RequireAuth` + `CreditsPage`, never `CanvasLayerRedirect`); (b) behavioral render of the ledger at `/songs/s1/credits` (heading, "Every contribution remembered.", copy-credits export button). 2/2 passing.
- Committed to `main` and pushed to origin (E4 files only: this progress log + the recreated test).
